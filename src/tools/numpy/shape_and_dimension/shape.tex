Shape is a tuple of dimension 2. shape can be (3,) or (3,1) and this two are different.

\begin{minted}{python}
        a1 = np.array(
                    [1],
                    [3],
                    [6])
        print(a1.ndim) # prints 1
        
        a2 = np.array([[1],[3],[6]])
        print(a2.ndim) # prints 2
\end{minted}