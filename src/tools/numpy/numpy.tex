\section{Numpy}
    It can also handel any type even strings
    \subsection{Creating}
    \paragraph{np.array()}
        Make a copy unless copy argument is set to False
        \begin{minted}{python}
        \end{minted}
    \paragraph{np.asarray()}
        asarray doesnt make a copy if the input itself is ndarray with the same dtype

